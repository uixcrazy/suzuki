import { makeStyles } from '@material-ui/core/styles';

const styles =theme => ({
  main: {
    height: '100%',
    // marginTop: 'var(--height-header)',
    paddingBottom: 150,

    position: 'relative',
    zIndex: '1',
  },
});

export default styles;
