import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
  return (
    <div>
      default header!!!
    </div>
  );
};

Header.propTypes = {

};

export default Header;